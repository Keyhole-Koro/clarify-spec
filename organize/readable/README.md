# Organize Readable Docs

このディレクトリは `organize` を人間が素早く理解するための読み物を置く。  
仕様の正本（MUST/契約）は `organize/specs/` と `organize/agents/` を参照する。

## 読み順（最短）

1. `organize/readable/organize-guide.md`

## ルール

* ここは説明用。正本契約を上書きしない
* 数値、冪等、lease、CAS の最終定義は `organize/specs/pipeline/*.md` を参照する
* Agent 個別の未決定事項は `organize/agents/*.md` を参照する
